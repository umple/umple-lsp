class /*@up_class*/A {
  // Untyped param (Java-like tolerance)
  public A /*@up_method*/createWarehouse(name) {}

  // Mixed typed/untyped param list from real corpus
  void /*@up_bulk*/createBulk(productType, Integer number, serialNumber) {}

  // Normal typed params still work
  void foo(Integer x, String y) {}

  attr;
}
